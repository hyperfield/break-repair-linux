# Boot

Boot scenarios focus on recoverable early-startup and next-boot problems such
as the wrong default target, missing kernel arguments, dracut mistakes, and
unsafe `fstab` entries.

For a guided sequence that mixes the related boot tasks and scenarios, see
`docs/rhel-boot-learning-path.md`.
