# Default Target Set To Rescue

- Check the saved default target, not only the current running target.
- A rescue default target is a boot configuration issue, even if the current session is already up.
