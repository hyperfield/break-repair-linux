# Locked Support User

Ticket: the local support account `labassist` should be usable for local login,
but the account's password is locked.

Target state:

- user `labassist` exists
- login shell is `/bin/bash`
- the password field for `labassist` is not locked
