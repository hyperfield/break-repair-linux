# Locked Support User

1. Confirm the user exists with `id labassist`.
2. Inspect the account with `passwd -S labassist` or `/etc/shadow`.
3. Unlock the password field while keeping the shell as `/bin/bash`.
4. Verify the password field no longer starts with a lock marker.
