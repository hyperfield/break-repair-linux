# Expired Backup User

Ticket: the local backup account `labbackup` should be usable again. The account
itself should remain present.

Target state:

- user `labbackup` exists
- the account expiration date is cleared or set so the account is no longer
  expired
