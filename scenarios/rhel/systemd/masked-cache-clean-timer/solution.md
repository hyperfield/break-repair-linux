# Masked Cache Clean Timer

1. Inspect the timer state with `systemctl status lab-cache-clean.timer`.
2. Confirm whether the timer is masked, disabled, or enabled.
3. Unmask the timer, then enable and start it again.
4. Verify the timer remains active and keeps its intended schedule.

