# Broken Dummy Profile

- Use `nmcli connection show` to compare the saved profile with the target state.
- This scenario is about repairing the profile, not creating a new one with a different name.
