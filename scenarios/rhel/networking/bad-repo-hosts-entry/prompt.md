# Bad Repo Hosts Entry

Ticket: the local repo hostname is resolving to the wrong address on this host.

Target state:

- `repo-rhel-lab.example.test` resolves to `192.0.2.50`
- there is exactly one local hosts-file entry for that name
