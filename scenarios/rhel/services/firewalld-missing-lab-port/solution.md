# Firewalld Missing Lab Port

1. Confirm firewalld is running.
2. Add `8080/tcp` to the active firewall configuration.
3. Add `8080/tcp` to the permanent firewall configuration.
4. Verify both runtime and permanent firewalld state.
