# Create Service Drop-In Override

- The verification script checks the merged unit view, not only raw files on disk.
- `systemctl edit` is the natural tool if you want to practice the workflow used on live systems.
