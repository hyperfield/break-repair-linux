# Set Default Multi-User Target

Configure the system so the default boot target is:

```text
multi-user.target
```

This is a boot-target configuration task. Verification checks the saved default
target for the next boot; it does not reboot the VM.
