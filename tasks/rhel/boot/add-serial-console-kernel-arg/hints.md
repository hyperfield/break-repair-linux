# Add Serial Console Kernel Arg

- On RH-compatible systems, `grubby` is the normal tool for persistent kernel command-line changes.
- Verification checks the saved default boot entry, not the currently running kernel command line.
