# Create Disabled BaseOS Repo File

1. Create `/etc/yum.repos.d/lab-baseos.repo`.
2. Add the requested repo id, name, baseurl, and boolean options exactly.
3. Confirm the saved file contents before running verification.
