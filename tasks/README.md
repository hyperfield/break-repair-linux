# Tasks

Tasks are organized by distro family first, then by category:

```text
tasks/<family>/<category>/<slug>/
```

Use tasks for build/configure/verify exercises rather than break/repair drills.

